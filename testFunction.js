// test these;

var addOne = function(x) {
    return x + 1;
  };
  var six = addOne(5)
  // console.log(six) output = 6
  // returns x + 1
  // bit like how object methods are defined
  
  window.onload = function() {
    alert("the page is loaded")
  }
  //again the function doesn't define its own name

  /*observation: functions can be defined w/o names and used in
  places where you might expect an expression */
  